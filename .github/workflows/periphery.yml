name: Periphery (Detect Unused Code in PR)

on:
  pull_request:
    branches:
      - main
      - develop
      - master

jobs:
  periphery:
    runs-on: macos-latest
    permissions:
      pull-requests: write
      contents: read
    timeout-minutes: 10
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Select Xcode Version
        run: sudo xcode-select -s /Applications/Xcode.app

      - name: Show Xcode Project Info
        run: xcodebuild -list -json -project GithubActionSample.xcodeproj

      - name: Clean Xcode Build
        run: xcodebuild clean -project GithubActionSample.xcodeproj

      - name: Install Periphery
        run: brew install peripheryapp/periphery/periphery

      - name: Run Periphery
        run: |
          periphery scan \
            --project GithubActionSample.xcodeproj \
            --schemes GithubActionSample \
            --targets GithubActionSample \
            --format checkstyle \
            --relative-results \
            --disable-update-check \
            --quiet
        continue-on-error: false 

      - name: Comment on PR if Unused Code is Found
        if: ${{ failure() }}
        uses: thollander/actions-comment-pull-request@v2
        with:
          message: "🚨 Periphery detected **unused code** in this PR! Please review and clean up unnecessary declarations."
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
